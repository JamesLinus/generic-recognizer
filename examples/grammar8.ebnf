!
! json's grammar.
!

object* = "{" [ pairs ] "}" ;
pairs = pair { "," pair } ;
pair = #STR ":" value ;
value = #STR | #NUM | "true" | "false" | "null" | object | array ;
array = "[" [ elements ] "]" ;
elements = value { "," value } ;

.
